include <stdio.h>

main()
{
	float i,S,T;
	int k,N;
	printf("write quantity N:\n");
	scanf("%d",&N);
	i=1.0;
	S=0.0;
	for(k = 0; k <= N; k++)
	{
		T = i * (k+3)/(1+ k*3);
		S=S+T;
		i = i*(-1);
	}
	printf("%f",S);
	return 0;
}
